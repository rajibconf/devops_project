services:
  clean_work:
    image: rajibconf/clean_work:latest
    ports:
      - "80:80"
    networks:
      - clean_work_net_ov
    deploy:
      mode: replicated
      replicas: 2

networks:
  clean_work_net_ov:
    driver: overlay
