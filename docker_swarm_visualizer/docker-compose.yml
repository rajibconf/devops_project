services:
  visualizer:
    image: dockersamples/visualizer:latest
    container_name: swarm_visualizer
    ports:
      - "8080:8080"
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock"
    deploy:
      placement:
        constraints:
          - node.role == manager
